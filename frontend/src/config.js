// Endereço do backend FastAPI. Fixo em localhost porque o projeto roda tudo
// local (sem build step/variáveis de ambiente no frontend) — pra apontar
// pra produção, seria só trocar essa string.
export const URL_API = "http://localhost:8000";
